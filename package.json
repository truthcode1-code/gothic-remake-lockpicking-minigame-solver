{
  "name": "lockpick-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5005 --strictPort",
    "build": "vite build",
    "test": "node --test solver.test.mjs share.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
